class Solution(object):
    def merge(self, intervals):
        intervals.sort()
        s=[]
        for i in intervals:
            if s and s[-1][1]>=i[0]:
                s[-1][1]=max(s[-1][1],i[1])
            else:
                s.append(i)
        return s
        
