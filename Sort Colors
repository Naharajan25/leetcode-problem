class Solution:
    def sortColors(self, nums: List[int]) -> None:
        c=[0]*3
        for i in nums:
            c[i]+=1
        id=0
        for i in range(3):
            for j in range(c[i]):
                nums[id]=i
                id+=1
        return nums
        
